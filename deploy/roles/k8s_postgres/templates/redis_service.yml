apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: default
  labels:
    app: catchit-redis
spec:
  selector:
    app: catchit-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379