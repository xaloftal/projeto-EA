apiVersion: v1
kind: Service
metadata:
  name: frontend-loadbalancer
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: catchit-frontend
  ports:
    - port: 9000
      targetPort: 80